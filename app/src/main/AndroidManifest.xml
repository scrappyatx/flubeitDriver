<?xml version="1.0" encoding="utf-8"?><!--   ~ Copyright (c) 2017. scrapdoodle, LLC.  All Rights Reserved -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.flube.driver">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".userInterfaceLayer.testActivities.PreStartupActivity"
            android:launchMode="singleTask"
            android:noHistory="true">
            <!-- android:theme="@style/AppTheme.Fullscreen"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".dataLayer.services.ServerCommService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="true" />
        //android:process=":ServerCommService"/>
        <activity android:name=".userInterfaceLayer.activities.OfferClaimActivity" />
        <activity android:name=".userInterfaceLayer.activities.LoginActivity" />
        <activity android:name=".userInterfaceLayer.activities.MainActivity" />
        <activity android:name=".userInterfaceLayer.testActivities.AblyTestActivity" />
        <activity android:name=".userInterfaceLayer.activities.StartupActivity"></activity>
    </application>

</manifest>